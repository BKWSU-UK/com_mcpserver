name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Validate tag matches manifest version
        run: |
          TAG_VERSION="${GITHUB_REF_NAME#v}"
          MANIFEST_VERSION="$(grep -oP '<version>\K[^<]+' mcpserver.xml)"
          if [[ "$TAG_VERSION" != "$MANIFEST_VERSION" ]]; then
            echo "::error::Git tag v${TAG_VERSION} does not match mcpserver.xml version ${MANIFEST_VERSION}."
            echo "Bump <version> in mcpserver.xml, add a ## ${TAG_VERSION} section to CHANGELOG.md, commit, then create the tag on that commit."
            exit 1
          fi

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: "8.1"
          tools: composer

      - name: Build release package
        run: ./build.sh

      - name: Resolve version
        id: version
        run: |
          VERSION="$(grep -oP '<version>\K[^<]+' mcpserver.xml)"
          echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          echo "package=com_mcpserver-${VERSION}.zip" >> "$GITHUB_OUTPUT"

      - name: Extract release notes
        id: notes
        env:
          VERSION: ${{ steps.version.outputs.version }}
        run: |
          awk -v version="$VERSION" '
            $0 ~ "^## " version " " { capture=1; next }
            capture && /^## / { exit }
            capture { print }
          ' CHANGELOG.md > release-notes.md

          if [[ ! -s release-notes.md ]]; then
            echo "No changelog section found for version ${VERSION}." > release-notes.md
          fi

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          name: MCP Server for Joomla v${{ steps.version.outputs.version }}
          body_path: release-notes.md
          files: |
            ${{ steps.version.outputs.package }}
            com_mcpserver.mcpb
          generate_release_notes: false

      - name: Record package checksum in update feed
        env:
          VERSION: ${{ steps.version.outputs.version }}
          PACKAGE: ${{ steps.version.outputs.package }}
        run: |
          # The published artifact is the one built above; hash it and write the
          # checksum into update.xml on master (the feed Joomla actually reads),
          # so clients no longer warn about a missing integrity checksum.
          SHA256="$(sha256sum "$PACKAGE" | cut -d' ' -f1)"
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git fetch origin master
          # build.sh rewrites update.xml/changelog.xml in the tag checkout; discard
          # those local edits (-f) or the branch switch aborts. Only the zip (untracked)
          # and the SHA256 computed above are needed from the build.
          git checkout -f -B master origin/master
          python3 scripts/sync_update_entry.py update.xml "$VERSION" "$SHA256"
          if git diff --quiet -- update.xml; then
            echo "update.xml already current for v${VERSION}"
          else
            git add update.xml
            git commit -m "Add SHA-256 checksum for v${VERSION} to update feed"
            git push origin master
          fi
